<div class="container">
    <h1 class="mt-4 mb-4">Gracias por realizar tu compra, {{ticket.purchaser.fullname}}</h1>

    <div class="card">
        <div class="card-header">
            Código de Compra: {{ticket.code}}
        </div>
        <div class="card-body">
            <h5 class="card-title">Detalle de Compra</h5>
            <p class="card-text">Total Pagado: ${{ticket.amount}}</p>
            <p class="card-text">Purchaser: {{ticket.purchaser.fullname}}</p>
            <p class="card-text">Email: {{ticket.purchaser.email}}</p>
        </div>
    </div>

    <a href="/products" class="btn btn-primary mt-4">Volver a Productos</a>
</div>